import 'package:flutter/material.dart';

class LoginWidget{
  Widget loginwidget({
       BuildContext? context,
       String?  logincontroller,

     })
     
     { 
      return Scaffold(
       body:       Container(
        
       ) 

    );
  }
}